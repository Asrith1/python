n=int(input("enter n value: "))
def Fibonacci(n) :
	if n<=1 :
		return n
	else :
		return (Fibonacci(n-1)+Fibonacci(n-2))
if n<=0 :
	print("enter positive number")
else :
	for i in range(n) :
		print(Fibonacci(i))
